import 'package:flutter/material.dart';

class Complainstatus extends StatelessWidget {
  const Complainstatus({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}