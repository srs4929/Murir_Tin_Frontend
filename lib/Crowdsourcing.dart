import 'package:flutter/material.dart';

class Crowdsourcing extends StatelessWidget {
  const Crowdsourcing({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}